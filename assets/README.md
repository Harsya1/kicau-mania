Place your audio and GIF assets in this folder.

Default names expected by main.py:
- kicau-mania.mp3
- kicau-mania.gif

Update `AUDIO_FILE` and `GIF_FILE` in `main.py` if you use different names.

Transparent GIFs are supported. If your overlay has a transparent background, keep the alpha channel intact when exporting the file.
